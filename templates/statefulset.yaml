apiVersion: apps/v1beta2
kind: StatefulSet
metadata:
  annotations:
{{ toYaml .Values.deployment.annotations | indent 4 }}
  creationTimestamp: 2019-02-07T12:10:46Z
  generation: 2
  labels:
{{ include "artemis-helm.labels.standard" . | indent 4 }}
  name: {{ include "artemis-helm.fullname" . }}
spec:
  podManagementPolicy: OrderedReady
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      workload.user.cattle.io/workloadselector: statefulSet-artemis-helm-artemis-helm
  serviceName: artemis-helm
  template:
    metadata:
      annotations:
        cattle.io/timestamp: 2019-02-07T12:11:27Z
      creationTimestamp: null
      labels:
        workload.user.cattle.io/workloadselector: statefulSet-artemis-helm-artemis-helm
    spec:
      containers:
      - image: docker.io/alpine:3.9
        imagePullPolicy: {{ .Values.image.pullPolicy | quote }}
        name: artemis-helm
        resources: {}
        securityContext:
          allowPrivilegeEscalation: false
          privileged: false
          procMount: Default
          readOnlyRootFilesystem: false
          runAsNonRoot: false
        stdin: true
        tty: true
      dnsPolicy: ClusterFirst
      initContainers:
      - command:
        - echo
        - Running
        - the
        - Init
        - Container.....
        image: docker.io/alpine:3.9
        imagePullPolicy: {{ .Values.image.pullPolicy | quote }}
        imagePullSecrets: {{ toYaml .Values.image.pullSecrets }}
        name: artemis-helm-init
        resources: {}
        securityContext:
          allowPrivilegeEscalation: false
          capabilities: {}
          privileged: false
          procMount: Default
          readOnlyRootFilesystem: false
          runAsNonRoot: false
        stdin: true
        tty: true
